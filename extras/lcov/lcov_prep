#!/bin/bash

set -e

cd build-root
rm -rf html
# Remove all .gcda files
lcov --zerocounters --directory .
# Produce an info file containing zero coverage for all source files
lcov --capture --initial --directory . --output-file zero.info
