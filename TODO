

macip - egress + ARP + tests.

user can fix the engine.

change the update so its a 'commit' style;
  user gets old list, modifies it, then commits the new.

re-write classifier for src-ip-mac

SETS.